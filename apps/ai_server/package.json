{
  "name": "ai_server",
  "version": "1.0.0",
  "scripts": {
    "dev": "[ ! -d .venv ] && python3 -m venv .venv && .venv/bin/pip install -r requirements.txt; cd ../.. && PYTHONPATH=apps apps/ai_server/.venv/bin/uvicorn ai_server.main:app --reload --port 8000",
    "install:python": "python3 -m venv .venv && .venv/bin/pip install -r requirements.txt"
  }
}
