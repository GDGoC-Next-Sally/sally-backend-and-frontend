{
  "permissions": {
    "allow": [
      "Bash(xargs grep -l \"chat\\\\|socket\\\\|sse\\\\|livechat\" -i)",
      "Bash(curl -s http://localhost:8000/docs)",
      "PowerShell(try { Invoke-WebRequest -Uri \"http://localhost:8000/\" -TimeoutSec 2 -UseBasicParsing | Select-Object StatusCode } catch { $_.Exception.Message })"
    ]
  }
}
